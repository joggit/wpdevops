# Production WordPress app (wpdevops)
# Copy to .env and set values. Example: cp env.example .env
#
# IMAGE: Use the official WordPress image from Docker Hub, or your own image.
#   - wordpress:latest                    (default; official image from Docker Hub)
#   - YOUR_DOCKERHUB_USER/wordpress-app:latest   (your image, e.g. from wordpress repo CI)
# For private Docker Hub images: docker login

IMAGE=wordpress:latest
PORT=9080
WP_DB_USER=wordpress
WP_DB_PASSWORD=changeme
WP_DB_NAME=wordpress
MYSQL_ROOT_PASSWORD=changeme
